{
  "name": "cbm-cartogram",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-contained D3 renderer that maps any repository's regions, import edges, and test edges from a code-base-mapper bundle.",
  "scripts": {
    "build:data": "node tools/normalize-inventory.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000"
  }
}
