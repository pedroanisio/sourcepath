openapi: "3.0.0"
info:
  title: Fixture API
  version: 1.0.0
paths:
  /users:
    get:
      responses:
        "200":
          $ref: "./responses.yaml#/OK"
components:
  schemas:
    User:
      $ref: "./schemas.yaml#/User"
---
kind: Config
metadata:
  name: second-doc
