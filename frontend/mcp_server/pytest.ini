[pytest]
testpaths = tests
addopts =
    --cov=frontend/mcp_server
    --cov-config=frontend/mcp_server/.coveragerc
    --cov-report=term-missing
    --cov-fail-under=90
filterwarnings =
    ignore::DeprecationWarning:rdflib
