fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
rdflib>=7.0,<8.0
# Rust-backed store for the MCP sparql tool (rdflib fallback exists but
# cannot fit the 10 s dispatch budget on large bundles).
pyoxigraph>=0.5,<1.0
pyshacl>=0.27
numpy>=2.0,<3.0
pydantic>=2.7,<3.0
